export interface    DataCustomer {
        name?: string,
        address?: string,
        status?: boolean,
        email?: string,
        phone?: string,
        isRo?: boolean,
        income?: number,
        dueDate: Date,
        registeredSince?: Date

}